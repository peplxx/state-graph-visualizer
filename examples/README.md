# Example graphs

Generated with the [GFP tool in libstgx](https://github.com/peplxx/libstgx/tree/main/examples/gfp)
for the processor counts and task parameters specified in each YAML file, with
state pruning disabled (`kPrune = false`). The YAML exports can be loaded directly
into the visualizer.
